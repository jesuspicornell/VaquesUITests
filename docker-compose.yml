version: "3.7"
services:
  frontend:
    build:
      context: vacawasm
    ports:
      - "3000:80"
    networks:
      - xarxa
  rest:
    build:
      context: vaquesrest
      dockerfile: Dockerfile
    ports:
      - 4567:4567
    networks:
      - xarxa

networks:
  xarxa:
